node_modules/

# Biến môi trường / khoá API (Telegram bot token, Anthropic key...)
.env
.env.*
!.env.example

# Chat id Telegram đã bật cảnh báo — dữ liệu cá nhân của từng máy
data/alert-chats.json
data/open-calls.json
data/monitor-state.json

# Log & file tạm
*.log
npm-debug.log*
.DS_Store
Thumbs.db

# models/ ĐƯỢC commit để giao diện browser dùng model đóng gói sẵn mà không
# phải tự train.
# Nhớ chạy `npm run models:index` sau khi thêm/xoá model.
